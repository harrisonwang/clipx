[package]
name = "clipboard"
version = "0.1.0"
edition = "2024"
description = "Copy a file to the system clipboard"
license = "MIT OR Apache-2.0"

[dependencies]
anyhow = "1"
arboard = { version = "3", default-features = false }

[[bin]]
name = "copy"
path = "src/main.rs"
